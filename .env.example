# PeerWave Simple Deployment Configuration
# Copy this file to .env before deploying with docker-compose.yml
#
# Quick Setup:
# 1. Generate secrets (SESSION_SECRET, LIVEKIT_API_KEY, LIVEKIT_API_SECRET)
# 2. Run: docker-compose up -d

# ============================================
# Security Credentials (CHANGE THESE!)
# ============================================
# Generate with: openssl rand -base64 32
SESSION_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_MINIMUM_32_CHARS

# LiveKit API Credentials
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# ============================================
# Server Configuration (Optional)
# ============================================
NODE_ENV=development
PORT=3000

# ============================================
# LiveKit Configuration
# ============================================
# TURN domain (use localhost for local dev)
LIVEKIT_TURN_DOMAIN=localhost

# ============================================
# Email Configuration (Optional)
# ============================================
# For meeting invitations and notifications
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_FROM="PeerWave" <noreply@yourdomain.com>

# ============================================
# Admin Users (Optional)
# ============================================
# Comma-separated list of admin email addresses
# ADMIN_EMAILS=admin@example.com
