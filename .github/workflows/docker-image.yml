name: Publish Docker image (DEPRECATED - Use docker-build-web.yml)
permissions:
  contents: read

# This workflow is deprecated and kept for reference only
# Use docker-build-web.yml instead, which includes Flutter web build

on:
  # Disabled - use docker-build-web.yml instead
  workflow_dispatch:

jobs:
  deprecated:
    name: This workflow is deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️  This workflow is DEPRECATED"
          echo "Use docker-build-web.yml instead"
          echo "It includes Flutter web client build before Docker image creation"
          exit 1
